# Settings to use for the limited endpoint 

# Set allowed rate (int > 0)
RATE = 10
# Set period for allowed rate [second,minute,hour,day,month,year]
RATEPERIOD="second"

# Set allowed quota (int > 0)
QUOTA=10000
# Set period for allowed quota [second,minute,hour,day,month,year]
QUOTAPERIOD="day"

# Set max concurrent requests (int > 0)
MAXCONCUR=10

# Enable throttle (double response time) after rate/quota exceeded (bool)
THROTTLE=False

# Add a randomized delay of up to this many seconds (None or int > 0) - comment out for None
MAXRANDDELAY = 3